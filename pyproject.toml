[project]
name = "dashboard-obras-backend"
version = "0.1.0"
description = "Backend FastAPI para dashboard gerencial de obras integrado ao Sankhya"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
